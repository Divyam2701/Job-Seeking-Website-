{
  "name": "job-seeking-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start-all": "cd frontend && npm install && npm run build && cd .. && npx concurrently \"cd Backend && npm install && node server.js\" \"cd frontend && npm run preview\""
  },
  "dependencies": {
    "axios": "^0.27.2",
    "cloudinary": "^1.32.1",
    "cookie-parser": "^1.4.5",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-fileupload": "^1.2.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.10.9",
    "validator": "^13.6.0",
    "bcrypt": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "concurrently": "^6.0.0"
  }
}